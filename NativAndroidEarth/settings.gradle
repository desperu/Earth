pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "NativAndroidEarth"
include ':app', ':unityLibrary'
project(':unityLibrary').projectDir = new File('../UnityEarth/androidBuild/unityLibrary1')
